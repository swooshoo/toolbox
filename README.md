# toolbox

May need to consider Selenium instead of Beautiful Soup.  LinkedIn has strict anti-scraping policies and often blocks scrapers.  If jobs prints an empty list, it could be due to LinkedIn serving dynamic content using JavaScript, which requests library cannot handle.